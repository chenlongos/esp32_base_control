# ESP32-C3 双路编码电机控制器 UART 协议文档 V1.0

## 基本参数

| 项目 | 值 |
|------|-----|
| 波特率 | 115200 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验 | 无 |

---

## 帧格式

```
[0xAA] [0x55] [CMD] [LEN] [PAYLOAD x LEN] [CHK]
```

| 字段 | 长度 | 说明 |
|------|------|------|
| 帧头 | 2 字节 | 固定 `0xAA 0x55` |
| CMD | 1 字节 | 命令/响应字 |
| LEN | 1 字节 | Payload 长度（字节数，可为 0）|
| PAYLOAD | LEN 字节 | 数据内容 |
| CHK | 1 字节 | XOR 校验：`CMD ^ LEN ^ PAYLOAD[0] ^ ... ^ PAYLOAD[N-1]` |

---

## 系统状态

| 状态值 | 状态名 | 蓝灯 | 说明 |
|--------|--------|------|------|
| 0x00 | UNINIT | 灭 | 上电初始状态 |
| 0x01 | IDLE | 常亮 | 初始化完成 |
| 0x02 | READY | 慢闪 500ms | 配置完成 |
| 0x03 | RUNNING | 快闪 100ms | 电机运行中 |
| 0x04 | SYS_ERROR | 双闪 | 系统错误 |

---

## 说明：心跳看门狗已移除（2026-09）

曾内置"300→800ms 无帧即断联停车"看门狗，因主机(SG2002 单核)瞬时
静默频繁误触发、与上层控制策略冲突，**已删除**：
- 固件不再因"静默超时"自动停车；电机只跟随主机显式指令
- 主机自动重连（ping 检测 ESP32 是否就绪）与上层停车策略负责链路恢复
- 安全提醒：主机进程被强杀且车在运动时，ESP32 会保持最后指令——需现场
  急停/断电兜底

---

## 主机 → ESP32 命令

### 0x01 INIT — 系统初始化

- **Payload**：无（LEN = 0）
- **作用**：复位编码器计数、停止双路电机，进入 IDLE 状态
- **响应**：ACK(0x01)
- **前置状态**：任意

**示例**
```
AA 55 01 00 01
```

---

### 0x02 CONFIG — 系统配置

- **Payload**（4 字节）：

| 字节 | 含义 | 类型 |
|------|------|------|
| [0:1] | PPR（每转脉冲数） | uint16 大端 |
| [2:3] | PWM 频率（Hz） | uint16 大端 |

- **作用**：更新编码器 PPR 和 PWM 频率，重新初始化驱动引脚，进入 READY 状态
- **响应**：ACK(0x02) 或 NACK
- **前置状态**：必须为 IDLE

**常用 PPR 值**
| 减速比 | PPR |
|--------|-----|
| 1:48 | 2496 |
| 1:90 | 4680 |

**示例**（PPR=2496, PWM_FREQ=20000）
```
AA 55 02 04 09 C0 4E 20 CHK
CHK = 0x02^0x04^0x09^0xC0^0x4E^0x20 = 0xEB
完整帧: AA 55 02 04 09 C0 4E 20 EB
```

---

### 0x10 SET_SPEED — 设置电机速度

- **Payload**（3 字节）：

| 字节 | 含义 | 类型 |
|------|------|------|
| [0] | 电机 ID（0=M1, 1=M2） | uint8 |
| [1:2] | 速度（-255~255，负数反转） | int16 大端 |

- **作用**：设置指定电机的 PWM 速度和方向，进入 RUNNING 状态
- **响应**：ACK(0x10) 或 NACK
- **前置状态**：READY 或 RUNNING

**示例**（M1 正转 200）
```
AA 55 10 03 00 00 C8 CHK
CHK = 0x10^0x03^0x00^0x00^0xC8 = 0xDB
完整帧: AA 55 10 03 00 00 C8 DB
```

**示例**（M2 反转 150）
```
速度 -150 = 0xFF6A
AA 55 10 03 01 FF 6A CHK
CHK = 0x10^0x03^0x01^0xFF^0x6A = 0x77
完整帧: AA 55 10 03 01 FF 6A 77
```

---

### 0x11 STOP — 自由停止（Coast）

- **Payload**（1 字节）：电机 ID（0=M1, 1=M2）
- **作用**：两路 PWM 均置 0，电机自由滑行
- **响应**：ACK(0x11) 或 NACK
- **前置状态**：READY 或 RUNNING

**示例**（停 M1）
```
AA 55 11 01 00 10
```

---

### 0x12 BRAKE — 刹车

- **Payload**（1 字节）：电机 ID（0=M1, 1=M2）
- **作用**：两路 PWM 均置 255，电机短接制动
- **响应**：ACK(0x12) 或 NACK
- **前置状态**：READY 或 RUNNING

**示例**（刹 M2）
```
AA 55 12 01 01 12
```

---

### 0x13 SET_SPEEDS — 同时设置双电机速度

- **Payload**（4 字节）：

| 字节 | 含义 | 类型 |
|------|------|------|
| [0:1] | M1 速度（-255~255） | int16 大端 |
| [2:3] | M2 速度（-255~255） | int16 大端 |

- **作用**：一条指令同时设置两个电机的 PWM 速度和方向，进入 RUNNING 状态
- **响应**：ACK(0x13) 或 NACK
- **前置状态**：READY 或 RUNNING

**示例**（M1=200, M2=-150）
```
速度 200 = 0x00C8，-150 = 0xFF6A
AA 55 13 04 00 C8 FF 6A CHK
CHK = 0x13^0x04^0x00^0xC8^0xFF^0x6A = 0xD4
完整帧: AA 55 13 04 00 C8 FF 6A D4
```

---

### 0x20 GET_RPM — 查询转速

- **Payload**（1 字节）：电机 ID（0=M1, 1=M2, **2=双路同时返回**）
- **响应**：一条或两条 RPM_DATA(0x90)
- **前置状态**：IDLE 及以上

**示例**（查询双路）
```
AA 55 20 01 02 23
```

---

### 0x21 GET_STATUS — 查询系统状态

- **Payload**：无（LEN = 0）
- **响应**：STATUS(0x91)
- **前置状态**：任意

**示例**
```
AA 55 21 00 21
```

---

### 0x22 GET_ENCODER — 读取编码器累计脉冲

- **Payload**：无（LEN = 0）
- **响应**：帧 cmd=0x22，8 字节 payload（M1、M2 各 4 字节 int32 大端）
- **作用**：读取左右编码器累计计数，供主机做里程计（配合轮径 D、轴距 L、PPR 换算位姿）
- **前置状态**：任意

**示例**
```
AA 55 22 00 22
```

---

### 0x23 MOVE_DISTANCE — 闭环距离 / 转向控制

- **Payload**（6 字节）：

| 字节 | 含义 | 类型 |
|------|------|------|
| [0] | dir：0=前进 1=后退 2=左转(原地) 3=右转(原地) | uint8 |
| [1] | speed：速度（1~100） | uint8 |
| [2:5] | target：目标量 | int32 大端 |

- **单位约定**（固件内部用轮径 D=62mm、轴距 L=160mm、PPR 换算成编码器计数）：
  - dir=0/1（直行）：target 单位为 **mm**
  - dir=2/3（原地转向）：target 单位为 **0.1°**（例如 90° 传 900）
- **作用**：以指定速度闭环前进/后退指定距离，或原地转向指定角度，到达后自动刹车
- **响应**：ACK(0x23) 或 NACK
- **前置状态**：READY 或 RUNNING

**换算公式**（轮径 D、轴距 L、编码器 PPR）：
```
车轮周长 C = π × D
直行计数         = mm × PPR / C
原地转向单轮计数 = (deg × π/180 × L/2) × PPR / C
```

**示例**（前进 500mm，速度 60）
```
target = 500 = 0x000001F4
AA 55 23 06 00 3C 00 00 01 F4 EC
```

**示例**（原地左转 90°，速度 40）
```
target = 90°×10 = 900 = 0x00000384
AA 55 23 06 02 28 00 00 03 84 88
```

---

### 0x32 HEARTBEAT — 兼容心跳（无断联语义）

- **Payload**：无（LEN = 0）
- **作用**：兼容命令，不改变电机状态
- **响应**：`STATUS(0x91)`（7 字节，见下）—— 与 `GET_STATUS` 同格式
- **前置状态**：任意

**示例**
```
AA 55 32 00 32
```

---

### 0xFF RESET — 复位

- **Payload**：无（LEN = 0）
- **作用**：停止双路电机，回到 UNINIT 状态
- **响应**：ACK(0xFF)
- **前置状态**：任意

**示例**
```
AA 55 FF 00 FF
```

---

## ESP32 → 主机 响应

### 0x80 ACK

- **Payload**（1 字节）：被响应的命令字

**示例**（响应 INIT）
```
AA 55 80 01 01 80
```

---

### 0x81 NACK

- **Payload**（2 字节）：

| 字节 | 含义 |
|------|------|
| [0] | 被响应的命令字 |
| [1] | 错误码 |

**错误码表**

| 错误码 | 含义 |
|--------|------|
| 0x01 | 当前状态不允许该命令 |
| 0x02 | 校验错误 |
| 0x03 | 参数非法（长度/范围/ID错误）|
| 0x04 | 未知命令 |

---

### 0x90 RPM_DATA — 转速数据

- **Payload**（3 字节）：

| 字节 | 含义 | 类型 |
|------|------|------|
| [0] | 电机 ID（0 或 1） | uint8 |
| [1:2] | 转速 RPM（正=正转，负=反转）| int16 大端 |

**示例**（M1 = 185 RPM）
```
AA 55 90 03 00 00 B9 CHK
```

---

### 0x91 STATUS — 系统状态

- **Payload**（5 字节）：

| 字节 | 含义 | 类型 |
|------|------|------|
| [0] | 系统状态值 | uint8 |
| [1:2] | M1 转速 RPM | int16 大端 |
| [3:4] | M2 转速 RPM | int16 大端 |

---

## 典型初始化流程

```
主机                          ESP32
  |--- INIT (0x01) ----------->|
  |<-- ACK (0x01) -------------|   蓝灯常亮

  |--- CONFIG (0x02)           |
  |    PPR=2496, FREQ=20000 -->|
  |<-- ACK (0x02) -------------|   蓝灯慢闪

  |--- SET_SPEED (0x10)        |
  |    M1, +200 -------------->|
  |<-- ACK (0x10) -------------|   蓝灯快闪

  |--- GET_RPM (0x20) M1=0 --->|
  |<-- RPM_DATA (0x90) --------|
```

---

## 硬件引脚对照

| GPIO | 功能 |
|------|------|
| GPIO3 | M1 IN2 (DRV8833 AIN2) |
| GPIO4 | M1 IN1 (DRV8833 AIN1) |
| GPIO5 | M1 编码器 A 相 |
| GPIO6 | M1 编码器 B 相 |
| GPIO1 | M2 IN1 |
| GPIO2 | M2 IN2 |
| GPIO7 | M2 编码器 A 相 |
| GPIO10 | M2 编码器 B 相 |
| GPIO8 | 板载蓝色 LED |
| GPIO20 | UART RX (USB-CDC) |
| GPIO21 | UART TX (USB-CDC) |

## 距离闭环结果上报（STATUS 回包扩展）

`GET_STATUS (0x21)` / `HEARTBEAT (0x32)` 的响应 `RSP_STATUS (0x91)` 现为 **7 字节**：

```
[ sysState(1) | rpm1(2) | rpm2(2) | distActive(1) | distResult(1) ]
```

- `distActive`: 0/1 —— 距离/角度闭环是否运行中
- `distResult`: 0=无/运行中, 1=正常到达目标 (2 保留未用)
- 主机在每个 10Hz 轮询（GET_STATUS）顺带读取，闭环完成 ≤100ms 内可见，无需额外轮询；
- 新 `CMD_MOVE_DISTANCE` / `CMD_INIT` 会把 distResult 清零；闭环到达目标时置 1。
- 主机侧须与固件同步升级：按固定 5B 缓冲解析的旧主机读不到 rpm 字段（帧长已 7B），请用 AKA-00 新固件。
